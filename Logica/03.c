#include <stdio.h>

int main ( ) {
    int inteiro, qdrado;

        printf("Digite um numero inteiro:");
        scanf("%d",&inteiro);
        qdrado = inteiro * 4;
        printf("Este é o quadrado do inteiro:%d",qdrado);
          return 0;
}